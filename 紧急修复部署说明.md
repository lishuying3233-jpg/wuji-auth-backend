# 三天激活码被显示为年卡：紧急修复说明

本修复包解决了两类会导致授权套餐错误的缺陷。第一，`activation_codes.durationDays` 曾有 **365 天默认值**；任何创建激活码时遗漏期限字段的路径，都会把实际授权写成 365 天。第二，订阅接口使用范围判断生成卡种名称，无法准确区分三天卡、周卡和季卡。

> 部署后，桌面端无需重新安装。它会在打开首页或重新启动后调用授权服务的订阅接口，并显示服务端返回的正确套餐名称。

| 修复项 | 修复后的行为 |
|---|---|
| 套餐名称映射 | 仅从唯一套餐目录作**精确匹配**：`3 → 三天卡`、`7 → 周卡`、`90 → 季卡`、`365 → 年卡`。 |
| 新发激活码 | 只允许 1、3、7、30、90、365 天，并要求发码流程显式传入期限。 |
| 数据库保护 | 新迁移移除了 `durationDays` 的 365 天默认值，遗漏期限将不再被静默当成年卡。 |
| 已受影响客户 | 授权管理列表中新增时钟按钮“修正授权期限”。输入 `3` 后，已激活卡会以其首次激活时间为基准重算到期时间。 |
| 回归保障 | 新增三天卡、周卡、季卡、年卡的精确映射测试。 |

## 立即部署步骤

请用此压缩包覆盖你当前的授权后端项目，然后按原有方式发布。优先执行包内已有迁移，**不要在本次紧急修复中运行会新建迁移文件的 `pnpm db:push`**。若你的平台不会自动运行数据库迁移，请在生产数据库中先执行本包的 `drizzle/0008_remove_duration_default.sql`，再发布应用代码。

```bash
pnpm install --frozen-lockfile
pnpm exec drizzle-kit migrate
pnpm build
pnpm start
```

如果你的发布平台自动从代码仓库构建，请提交本压缩包内的全部源代码即可；不要上传 `node_modules`。构建完成后，桌面端下次请求 `payment.getSubscription` 会获得精确的 `planName` 和 `durationDays`。

## 修复已误发给客户的三天卡

部署完成后，使用管理员账号进入授权管理页，找到该客户的激活码。在该行操作区点击**时钟图标**，输入 `3` 并确认。系统会将该卡修正为三天卡；如果它已经激活，到期日会自动更新为“首次激活时间 + 3 天”。随后让客户重启桌面端或回到首页刷新订阅信息即可。

> 这一步会同步修正**真实有效期**，而非只修改前端文字。因此，请仅对本应售卖为三天的受影响记录执行此操作。

## 已完成验证

| 检查项 | 结果 |
|---|---|
| TypeScript 类型检查 | 通过 |
| 三天卡精确映射回归测试 | 3 项通过 |
| 生产构建 | 通过 |
| 完整测试套件 | 受当前环境缺少数据库、Telegram 和链上浏览器 API 凭据影响，外部集成测试未能运行；本次新增的本地回归测试已单独通过。 |

