# 无极助手云端授权系统 TODO

## 1. 后端与数据库 (Phase 1)
- [x] 在 `drizzle/schema.ts` 中定义 `activation_codes` 表 (激活码, 状态, 机器码绑定, 时间戳)
- [x] 生成并执行数据库迁移 SQL
- [x] 在 `server/db.ts` 中添加激活码查询、创建和绑定的辅助函数

## 2. 管理后台 UI (Phase 2)
- [x] 设计梦幻渐变风格的全局 CSS 主题 (`client/src/index.css`)
- [x] 实现后台管理页面 (`client/src/pages/Admin.tsx`)：生成激活码、搜索、列表展示
- [x] 实现激活码状态切换 (启用/禁用) 和复制功能
- [x] 限制管理后台仅管理员 (Owner) 可进入

## 3. 联网验证 API (Phase 3)
- [x] 在 `server/routers.ts` 中实现 `publicProcedure.auth.verify` 接口
- [x] 逻辑：检查码是否存在 -> 检查是否禁用 -> 检查是否已绑定 -> 首次绑定机器码 -> 返回结果
- [x] 编写 Vitest 测试确保验证逻辑安全可靠

## 4. 桌面端联调 (Phase 4)
- [x] 修改桌面端 `electron/main.cjs`，将本地验证改为请求云端 API
- [x] 更新桌面端激活 UI，显示联网验证状态
- [x] 交付最终后台链接与桌面端更新包

## 5. 授权期限管理 (Phase 5)
- [x] 在 `drizzle/schema.ts` 中增加 `durationDays` 和 `expiresAt` 字段
- [x] 更新后台管理页面，增加期限选择 (1, 3, 7, 30, 90, 365 天)
- [x] 更新验证逻辑：首次激活计算过期时间，后续验证检查是否过期
- [x] 修改桌面端，在到期后强制重新弹出激活页面

## 6. 后台 UI 优化 (Phase 6)
- [x] 优化 Admin 页面布局比例，使其更居中紧凑
- [x] 增加授权统计概览 (总数、已激活、已过期)
- [x] 优化表格视觉密度，增强状态区分度 (不同状态使用不同配色)
- [x] 增加快速筛选功能 (全部/有效/过期)

## 7. 独立认证与权限管理 (Phase 7)
- [x] 在 `drizzle/schema.ts` 中新增 `admins` 表 (账号, 密码哈希, 角色: super/sub, 权限)
- [x] 实现自定义账号密码登录，替代 Manus OAuth
- [x] 增加子管理员管理页面：主管理员可创建、删除子管理员并分配权限
- [x] 预置主管理员账号 `wudideji001` / `admin001`

## 8. 授权功能增强 (Phase 8)
- [x] 实现激活码“续期”功能，直接延长 `expiresAt`
- [x] 实现激活码“批量生成”功能
- [x] 在桌面端主界面增加到期提醒横条 (显示剩余天数)

## 9. 管理后台多语言版 (Phase 9)
- [x] 管理后台增加简体中文与英文语言包
- [x] 管理后台增加语言切换控件并持久化选择
- [x] 将登录页、首页和授权码管理页面的可见文本接入多语言
- [x] 将续期、批量生成、子管理员、状态提示和操作反馈接入多语言
- [x] 为多语言切换与关键文案增加测试并完成后台界面验证
- [x] 保存仅后台多语言版本的项目检查点

## 10. 多语言收尾修正
- [x] 将后台剩余硬编码品牌、页脚、按钮提示接入语言包
- [x] 将 Admin 页所有 toast、确认文案和状态提示统一改为 t() 翻译键
- [x] 修正第 9 阶段多语言条目状态并重新验证
- [x] 保存明确包含后台多语言版的项目检查点

## 11. 稳定域名登录跳转修复
- [x] 移除后台未登录状态下触发 Manus OAuth 的自动跳转
- [x] 让稳定域名根路径和管理页统一进入自定义 `/login`
- [x] 验证自定义管理员登录、退出登录和未授权重定向
- [x] 保存登录跳转修复版本并提供重新发布步骤

## 12. 登录入口与链路验证补充
- [x] 将稳定域名根路径直接重定向到自定义 `/login`
- [x] 增加自定义管理员登录成功与失败的自动化测试
- [x] 增加退出登录和未授权访问管理页跳转的验证
- [x] 保存包含完整登录链路修复的检查点

## 13. 登录修复交付收尾
- [x] 增加可核实的 `/admin` 未登录跳转 `/login` 自动化验证
- [x] 在交付说明中明确当前检查点需要重新发布到稳定域名
- [x] 完成收尾验证后保存最终检查点

## 14. 线上稳定域名仍跳转旧 OAuth
- [x] 核查稳定域名实际加载的前端入口和版本
- [x] 排除浏览器缓存、旧部署或服务端入口配置问题
- [x] 确保稳定域名直接进入项目内 `/login` 并完成线上验证
- [x] 保存线上入口修复版本并更新访问说明

## M7社媒助手 品牌统一
- [x] 后台站点标题、登录页和管理页品牌文案统一更名为 M7社媒助手
- [x] 更新后台标题配置并完成页面验证
- [x] 保存后台品牌更名版本

## 15. 自定义激活码前缀
- [x] 后台生成区域增加可选的前缀输入框 (Prefix)
- [x] 生成逻辑改为 `PREFIX-RANDOM` 格式，若未填则使用默认随机前缀
- [x] 验证前缀输入合法性 (仅限大写字母、数字和连字符)
- [x] 确保前缀生成不会破坏现有验证、绑定、续期和禁用逻辑
- [x] 增加前缀激活码生成与验证测试
- [x] 保存包含前缀自定义功能的版本

## 16. 授权时间下拉框白屏修复
- [x] 确认期限 Radix Select 弹层存在白屏风险并定位到该组件路径
- [x] 修复期限弹层、选项值和页面布局兼容性并完成浏览器点击验证
- [x] 通过真实路由验证 1天、3天、7天、1个月、3个月、1年对应的 durationDays
- [x] 增加期限选择回归测试并保存后台修复版本

## 17. 授权期限交互验证补充
- [x] 在后台实际点击期限下拉框，确认不再白屏并检查中英文布局
- [x] 增加真实生成流程测试，覆盖 1/3/7/30/90/365 天的 durationDays
- [x] 保存本次期限下拉框修复后的最新检查点

## 18. 生成激活码白屏修复
- [x] 定位生成请求、参数校验或成功回调导致的白屏
- [x] 为生成 mutation 增加错误提示和边界参数保护
- [x] 通过路由测试验证自定义前缀、六种期限、单码和批量参数
- [x] 保存生成按钮修复版本并完成后台交付验证

## 19. 生成后 React DOM 白屏修复
- [x] 定位生成后列表刷新触发的 insertBefore DOM 异常
- [x] 增加生成参数边界校验和 mutation 错误提示
- [x] 降低浏览器扩展改写输入框对 React 渲染的影响
- [ ] 验证生成成功、失败、列表刷新和无扩展环境（需要用户本地实际复测）
- [x] 保存白屏修复版本并交付测试方法

## 20. 生成交互真实环境验证
- [ ] 在中文和 English 两种语言下分别点击期限并检查布局与生成交互（需要用户本地实际复测）
- [ ] 在有浏览器扩展和无扩展环境下分别测试生成激活码（需要用户本地实际复测）
- [ ] 若仍有 insertBefore 异常，继续隔离生成区即时 DOM 更新（待真实双环境复测后确认）
- [x] 完成真实交互验证后保存新的后台修复检查点

## 21. 激活码多选与后台管理优化
- [x] 为授权码表增加单行选择、多选和当前筛选结果全选
- [x] 增加批量删除入口、数量提示和二次确认
- [x] 增加批量删除后安全刷新与清空选择状态
- [x] 检查搜索筛选、空状态、加载状态和大列表操作体验
- [x] 增加批量删除接口测试与前端回归测试
- [x] 保存多选/全选和后台优化版本（检查点 473991d9）

## 22. 批量操作高可信验证补充
- [x] 通过真实 tRPC 测试断言 count 大于 1 时返回多条带前缀和期限的激活码
- [x] 通过真实 tRPC caller 测试 activation.deleteMany 的输入校验、去重计数和数据库委托
- [x] 通过可执行纯函数回归测试覆盖多选、全选、限制和清空选择逻辑
- [ ] 在中文/English 和有/无浏览器扩展环境分别复测生成流程（需要用户本地实际复测）
- [ ] 完成高可信验证后保存最终多选版本检查点（需完成双环境复测后保存）

## 23. 清空选择回归验证补充
- [x] 为清空选择新增可执行测试，覆盖 selectedIds 归零
- [x] 通过代码路径检查确认筛选、搜索和刷新均调用 clearLicenseSelection
- [ ] 保存包含清空选择验证的多选/全选版本（需在验证后单独保存检查点）
